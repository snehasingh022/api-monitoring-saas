import { PlaceholderPage } from '../components/PlaceholderPage';

export function HomePage() {
  return (
    <PlaceholderPage
      title="API Monitoring SaaS"
      description="Frontend scaffold is ready. Auth pages, dashboard, and monitor management will be built in the next steps."
    />
  );
}
